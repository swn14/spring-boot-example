server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/sessions
    username: dbuser
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
    show-sql: true
  main:
    web-application-type: servlet

app:
  sabrePseudoCityCode: W6J2
  sabreUsername: 7971
  sabrePassword: AOK2233